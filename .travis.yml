sudo: false
language: cpp
compiler:
  - gcc
  - clang
addons:
  apt:
    packages:
      - libncurses5-dev
      - libsdl1.2-dev
      - libportmidi-dev
      - libsndfile1-dev
      - libuv1-dev
script: make
